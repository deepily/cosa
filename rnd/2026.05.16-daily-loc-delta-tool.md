# Daily LoC Delta Tool — Plan

**Status**: 🟢 SHIPPED — all 5 test tiers green; package README written; filename default mode-aware (post-ship enhancement)
**Author**: María 🌸 session `3c9fce51` (serialized from `~/.claude/plans/resilient-soaring-turtle.md`)
**Repo**: CoSA submodule (`src/cosa/`) — commit lands in CoSA-context session
**User-facing docs**: [`src/cosa/repo/git_loc_delta/README.md`](../repo/git_loc_delta/README.md) — comprehensive scenarios + CLI reference

## Context

Rick wants a per-day breakdown of lines added/deleted from the repo, broken down by file type, with two modes:

1. **Default (today)** — just today's commits: total added, total deleted, file-type breakdown.
2. **Branch range** — for every day Rick has been working on the current branch, show daily totals (added, deleted) + a per-day file-type summary.
3. **Bonus** — emit results as CSV so daily numbers can be tracked, pivoted, and graphed over time.

Why this exists: the existing `cosa/repo/branch_analyzer/` produces **branch-total** deltas via `git diff base...head`. It cannot bucket by date and cannot iterate per-commit. To answer "what did I do today" or "show me my daily velocity on this branch," we need a new entry point that walks `git log` and aggregates by commit date. The existing skill's classifiers and statistics helpers are reusable — only the orchestration and CSV writer are net-new.

## Approach

Build a new sibling under `cosa/repo/` that mirrors `branch_analyzer/`'s structure but is driven by `git log --numstat` (per-commit) instead of `git diff` (branch-total). This keeps separation of concerns clean — the existing `branch_analyzer` stays untouched and continues to answer "what did this whole branch change," while the new `git_loc_delta` answers "what changed when."

### Why a new package, not an extension of `branch_analyzer`

- `branch_analyzer` is hard-wired to three-dot `git diff base...head` and aggregates into a single bucket. Bolting per-commit iteration onto it would require restructuring `StatisticsCollector` to carry a `(date, file_type) → {added, deleted}` key shape, which would either break the existing public API or duplicate it conditionally. Cleaner to introduce a new package.
- Justified by the audit: the file/line classifiers and CSV-output gap are the only material reuse/build points; the rest of `branch_analyzer` is orthogonal to this use case.

## File Structure

```
src/cosa/repo/
  run_git_loc_delta.py            # CLI entry (mirrors run_branch_analyzer.py)
  git_loc_delta/
    __init__.py
    analyzer.py                    # GitLogLocDeltaAnalyzer — orchestrator
    git_log_parser.py              # subprocess-shells `git log --numstat`, yields per-file rows
    daily_aggregator.py            # buckets rows by (date, file_type) → {added, deleted}
    csv_writer.py                  # tidy-long CSV output
    report_formatter.py            # console + json (CSV handled by csv_writer)
    exceptions.py                  # GitCommandError, DateRangeError
```

**Reuse from `branch_analyzer/`** (import, do not copy):
- `cosa.repo.branch_analyzer.file_classifier.FileClassifier` — extension → language/file-type mapping
- `cosa.repo.branch_analyzer.exceptions.GitCommandError` — shared exception base

**Deliberately NOT reused initially**:
- `line_classifier.py` — code/comment/docstring split requires fetching each file's diff content per commit (expensive). Skip for v1; add `--classify-lines` flag later as opt-in if useful.

## CLI Surface

```
python -m cosa.repo.run_git_loc_delta [OPTIONS]

  --repo-path PATH         Repo to analyze (default: cwd)

  Date range (mutually exclusive):
  --today                  (default) Commits since today 00:00 local
  --since YYYY-MM-DD       Inclusive lower bound
  --until YYYY-MM-DD       Inclusive upper bound (default: today)
  --branch [BRANCH]        Range = merge-base(main, BRANCH)..BRANCH
                           BRANCH defaults to current branch.
                           Base ref configurable via --base (default: main).
  --base REF               Base ref for --branch mode (default: main)

  Filters:
  --include-merges         Include merge commits (default: exclude)
  --author EMAIL           Filter by commit author email (default: all)

  Output:
  --output FORMAT          console | json | csv (default: console)
  --save-output PATH       Write to file. For --output csv, defaults to:
                           {project_root}/io/git-loc-delta/{YYYY-MM-DD}-loc-delta.csv

  -v, --verbose            Echo git commands and per-commit progress to stderr
  --debug                  Verbose + full tracebacks
```

**Exit codes**:
- `0` — success
- `1` — git failure (any `GitCommandError`: command not found, non-zero return code, timeout)
- `2` — argument-parse error (raised by `argparse` automatically on bad flags)

## Algorithm

1. **Resolve date range** — `--today` (default) → since=today 00:00, until=now. `--branch` → since=author-date of `git merge-base <base> <branch>`, until=HEAD author-date. Explicit `--since`/`--until` override.
2. **Shell out to git log** with the canonical pattern from `branch_analyzer/git_diff_parser.py:114-147` (`subprocess.run`, `capture_output=True`, `text=True`, timeout, custom `GitCommandError`):
   ```
   git log --numstat --no-merges --date=short \
           --pretty=format:'COMMIT|%H|%ad|%aE' \
           --since=<since> --until=<until> [<rev-range>]
   ```
3. **Parse stream** in `git_log_parser.py` — for each `COMMIT|...` line, set current `(sha, date, author)`; for each `\d+\t\d+\tpath` line, yield a `FileChange(date, sha, path, added, deleted)`. Binary files (`-\t-\t...`) → skip.
4. **Classify file type** via existing `FileTypeClassifier.classify(path)` → `python | javascript | markdown | other | …`. (Classifier is constructed with the branch_analyzer config loaded via `ConfigLoader().load()`.)
5. **Aggregate** in `daily_aggregator.py` to a single `dict[(date, file_type)] → {added, deleted, files_touched}` plus a `dict[date] → {added, deleted}` totals view.
6. **Emit** per `--output`:
   - **console** — two tables: per-day totals, then per (date × file_type) breakdown. Use `du.print_banner` for headers (matches COSA idiom).
   - **json** — `{ days: [{ date, total_added, total_deleted, by_file_type: [...] }], summary: {...} }`
   - **csv** — tidy-long shape (see below).

## CSV Output Shape (the bonus feature)

**Tidy-long**, pivot-friendly, fixed schema regardless of file-type cardinality:

```csv
date,file_type,added,deleted,files_touched,commits
2026-05-15,python,128,42,7,3
2026-05-15,markdown,300,15,2,3
2026-05-15,javascript,50,8,1,3
2026-05-16,python,80,20,5,2
```

Why long not wide: new file types over time would add columns in a wide format, breaking downstream consumers. Long is stable, pivotable in pandas with one line, and graphs cleanly in matplotlib/Excel.

Implementation: `pandas.DataFrame(rows).to_csv(path, index=False)` — matches the COSA CSV convention.

**Default save location** (when `--output csv` without `--save-output`) — **mode-aware** (post-ship enhancement 2026-05-16):

- `--branch` mode → `{cu.get_project_root()}/io/git-loc-delta/{repo}-{branch-slug}-loc-delta.csv`
  (stable per-branch filename; daily reruns overwrite in place until the branch lands)
- `--today` / `--since`/`--until` mode → `{cu.get_project_root()}/io/git-loc-delta/{YYYY-MM-DD}-loc-delta.csv`
  (date-stamped for archival snapshots)

Directory auto-created if missing. The mode-aware default replaces the original single-pattern default during the post-ship "documentation + filename-flip" iteration (Rick voice-approved 2026-05-16 via `ask_multiple_choice`).

> **Convention alignment (Pass 1 F1)**: `io/` is the established artifact-output convention across the project (`io/decision-proxies/`, `io/swe-team/reports/`, `io/deep-research/`, `io/commons/`). Aligning here avoids inventing a parallel top-level `reports/` tree.

## Console Output Sketch

```
─────────────────────────────────────────────────────
  Daily LoC Delta — 2026-05-15 .. 2026-05-16
  Branch: wip-v0.1.7-2026.04.23-tracking-lupin-work
─────────────────────────────────────────────────────

Daily Totals
  Date         Added    Deleted   Net     Commits
  2026-05-15     478        65   +413          3
  2026-05-16      80        20    +60          2
  TOTAL          558        85   +473          5

By Date × File Type
  Date         File Type    Added   Deleted   Files
  2026-05-15   python         128        42       7
  2026-05-15   markdown       300        15       2
  2026-05-15   javascript      50         8       1
  2026-05-16   python          80        20       5
```

## Reuse Map (verified file:line refs)

| Need | Source | Reuse strategy |
|------|--------|---------------|
| File extension → file_type | `cosa/repo/branch_analyzer/file_classifier.py` | Import `FileTypeClassifier` (loads via `ConfigLoader().load()`) |
| Custom git exception | `cosa/repo/branch_analyzer/exceptions.py` | Import `GitCommandError` |
| Subprocess git pattern | `cosa/repo/branch_analyzer/git_diff_parser.py:114-147` | Copy idiom (capture_output, text, timeout) |
| CLI scaffolding | `cosa/repo/run_branch_analyzer.py:69-156` | Copy `create_parser()` + `main()` shape |
| `quick_smoke_test()` template | `cosa/repo/branch_analyzer/analyzer.py` | Match signature + ✓/✗ pattern |
| CSV writing | `cosa/agents/todo_list_agent.py:89-90` (uses `to_csv(index=False)` for in-memory prompt-string embedding) | Use `pandas.DataFrame.to_csv(path, index=False)` — same API extended to disk file writes |
| Project root resolution | `cosa/utils/util.py:626` `get_project_root()` | Call `cu.get_project_root()` for default output dir |

## Verification Plan

After implementation:

1. **py_compile** every new `.py` file (post-edit verification mandate):
   ```bash
   python -c "import py_compile; py_compile.compile('src/cosa/repo/run_git_loc_delta.py', doraise=True)"
   # (repeat for each git_loc_delta/*.py)
   ```

2. **Import chain check**:
   ```bash
   PYTHONPATH=/mnt/DATA01/include/www.deepily.ai/projects/lupin/src python -c \
     "from cosa.repo.git_loc_delta.analyzer import GitLogLocDeltaAnalyzer; print('OK')"
   ```

3. **Smoke test (`quick_smoke_test()` in `analyzer.py`)** — runs `git log` over the COSA repo for the last 7 days, asserts dict shape, prints ✓/✗.

4. **Live verification on the current branch** — three console runs Rick can eyeball:
   ```bash
   # Mode 1: today only (default)
   python -m cosa.repo.run_git_loc_delta

   # Mode 2: whole current branch
   python -m cosa.repo.run_git_loc_delta --branch

   # Bonus: CSV export
   python -m cosa.repo.run_git_loc_delta --branch --output csv
   ```
   Cross-check sample numbers against `git log --numstat --since=...` by hand on one day.

5. **Unit tests** (`src/tests/unit/test_git_loc_delta.py`, 3 tests, **required v1** per Testing Ownership Mandate — CLAUDE.local.md "USER IS NEVER A TESTER"):
   - `test_parser_handles_binary_files` (sees `-\t-\tfile.png`, skips it)
   - `test_aggregator_buckets_by_date_and_type`
   - `test_csv_long_shape_is_stable_across_file_types`

> **Smoke-test scope (Pass 1 F8)**: per CoSA sub-CLAUDE.md ("always ask the user if they want a smoke test included for new modules"), one canonical `quick_smoke_test()` in `analyzer.py` covers the full parse→aggregate→format chain end-to-end. Adding per-module smokes to the 7 other files would be overkill — the analyzer-level smoke exercises them transitively. This decision is intentional, not an omission.

## Testing Tiers (Pass 1 F3)

Consolidated tier table — pass/fail to be reported in tabular form post-implementation per Testing Ownership Mandate.

| Tier | What runs | Venue | Discretion |
|---|---|---|---|
| **T1: py_compile** | `python -c "import py_compile; py_compile.compile(f, doraise=True)"` for each of the 8 new `.py` files | local | AI |
| **T2: import chain** | `from cosa.repo.git_loc_delta.analyzer import GitLogLocDeltaAnalyzer` resolves | local | AI |
| **T3: unit** | 3 tests in `src/tests/unit/test_git_loc_delta.py` (binary skip, date×type bucketing, CSV long-shape stability) | local | AI |
| **T4: smoke** | `quick_smoke_test()` in `analyzer.py` against current repo (7-day window) | local | AI |
| **T5: live CLI** | 3 modes: `python -m cosa.repo.run_git_loc_delta` (today), `--branch`, `--branch --output csv` | local | AI |

No `:7999` or `:8000` server tests needed — this is a pure CLI tool with no API/UI/WebSocket surface.

## Acceptance Criteria (Pass 1 F5)

**Correctness**
- AC1: Default invocation prints today's commits with daily totals + by-file-type table to console
- AC2: `--branch [BRANCH]` mode produces per-day breakdown from `git merge-base <base> <branch>` to HEAD
- AC3: `--since/--until` explicit-range mode overrides default
- AC4: `--output csv` writes tidy-long CSV (`date, file_type, added, deleted, files_touched, commits`) to default path `{project_root}/io/git-loc-delta/{YYYY-MM-DD}-loc-delta.csv`
- AC5: `--output json` produces nested-dict shape `{days: [...], summary: {...}}`
- AC6: Binary files (`-\t-\t...` rows from `git log --numstat`) excluded from totals
- AC7: Merge commits excluded by default; `--include-merges` opts in
- AC8: `GitCommandError` raised with command + return code + stderr context on git failure

**Coverage**
- AC9: All 8 new `.py` files pass `py_compile` (post-edit verification mandate)
- AC10: Import chain resolves clean
- AC11: `quick_smoke_test()` in `analyzer.py` runs successfully against current repo
- AC12: All 3 unit tests pass
- AC13: All 3 live CLI runs produce expected console/CSV output

**Style**
- AC14: All functions/methods carry Design-by-Contract docstrings (Requires/Ensures/Raises)
- AC15: Variable assignments use vertical `=` alignment per COSA style
- AC16: No defensive `getattr()` chains

**Edge cases**
- AC17: Empty commit range produces graceful empty output (no traceback, exit 0)
- AC18: Rename rows from `--numstat` handled deterministically per documented behavior in §"Edge Cases" below

## Sweep Check (Pass 1 F4)

Per `feedback_sweep_for_pattern_offenders`, verified no existing per-day git-log LoC tool overlaps this plan:

| Search | Path | Hits |
|---|---|---|
| `find … -name '*.py' \| xargs grep -l 'git log\|--numstat'` | `src/cosa/repo/` | 0 (only `branch_analyzer/` uses `git diff base...head`, not `git log`) |
| Same | `src/scripts/` | 1 false positive: `tfe_to_cc_phase3_live.py` uses `git log` for context-gathering, not LoC |

**Conclusion**: `branch_analyzer` is branch-total only (single `git diff`); this plan's per-commit `git log --numstat` walk is genuinely net-new functionality. No duplication risk.

## Edge Cases (Pass 1 F6)

| Edge case | Expected behavior | AC |
|---|---|---|
| **No commits in date range** (e.g. `--today` on a quiet weekend) | Print "No commits in range" banner, write zero-row CSV with just headers when `--output csv`, exit 0 | AC17 |
| **`--branch` on the base branch itself** (e.g. on `main` with `--base main`) | merge-base = HEAD → empty rev-range. Same handling as no-commits case. | AC17 |
| **Rename rows** (`git log --numstat` emits `R100\told\tnew` or `rename` lines depending on format) | With the chosen `--no-renames` flag NOT set: numstat emits the rename as separate add+delete entries with `added`/`deleted` counts of moved bytes. Default behavior keeps rename rows AS-IS (additive count). Documented in `git_log_parser.py` docstring. | AC18 |
| **Submodule paths** (e.g. running over Lupin with `src/cosa/` as a submodule) | `git log --numstat` on the parent does NOT descend into submodules by default. Submodule commits won't appear. Documented limitation. | (none — accept) |
| **Timezone for `--today`** | `--since "today 00:00"` uses git's interpretation, which respects local TZ. Documented in `--today` flag help. | (none — accept) |
| **Author filter as exact-match** | `--author EMAIL` is passed through to `git log --author=<value>` which matches as substring/regex. Document this in `--author` help. | (none — accept) |

## Out of Scope (v1)

- Code-vs-comment-vs-docstring split per day (requires fetching diff content per commit; expensive). Add `--classify-lines` later if needed.
- Author breakdown beyond `--author EMAIL` filter.
- Cumulative chart generation (matplotlib). CSV → external graphing is the bonus; plot rendering is a future step.
- Cross-repo aggregation. Scope is one repo per invocation.

## Critical Files to Modify / Create

**Create** (all new):
- `src/cosa/repo/run_git_loc_delta.py`
- `src/cosa/repo/git_loc_delta/__init__.py`
- `src/cosa/repo/git_loc_delta/analyzer.py`
- `src/cosa/repo/git_loc_delta/git_log_parser.py`
- `src/cosa/repo/git_loc_delta/daily_aggregator.py`
- `src/cosa/repo/git_loc_delta/csv_writer.py`
- `src/cosa/repo/git_loc_delta/report_formatter.py`
- `src/cosa/repo/git_loc_delta/exceptions.py`

**Modify**: none. Existing `branch_analyzer/` is imported, not edited.

## Estimated Size

- ~600-800 lines of new code across the 8 files
- Single-session implementation (no multi-phase ladder needed)
- No config-file dependency in v1 (CLI flags are enough)
